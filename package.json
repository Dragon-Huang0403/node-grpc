{
  "name": "node-grpc",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "client": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "proto": "grpc_tools_node_protoc --js_out=import_style=commonjs,binary:./ --grpc_out=grpc_js:./ ./src/proto/counter.proto"
  },
  "devDependencies": {
    "grpc-tools": "^1.12.4",
    "typescript": "^5.2.2",
    "vite": "^5.0.8"
  }
}
